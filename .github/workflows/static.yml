name: Deploy Static Site
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Generator
        run: python scripts/generator.py
      - name: Deploy
        uses: actions/deploy-pages@v4
        with: {artifact_name: github-pages-artifact, path: './docs'}
